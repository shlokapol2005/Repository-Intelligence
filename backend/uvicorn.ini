# uvicorn config — prevents .venv from triggering hot-reload loops
# Run with: uvicorn main:app --reload

[uvicorn]
reload_dirs = ["routers", "utils"]
reload_excludes = [".venv", "__pycache__", "*.pyc", "*.pyo", "parsing_result.json"]
